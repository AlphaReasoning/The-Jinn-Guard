[toolchain]
# Release reproducibility requires a concrete compiler version. Update this
# through an explicit PR when moving the release toolchain.
channel = "1.95.0"
profile = "minimal"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-linux-gnu", "aarch64-unknown-linux-gnu"]
